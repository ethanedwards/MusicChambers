//Sets up the variables for new objects which will be referenced at each call, handles values through max message function

//first variable is time, second must be non-zero to be interpreted as change message
//
//MAXMESSAGE(0.0, 1.0, slot, voices, alive, bamp)
print_off()